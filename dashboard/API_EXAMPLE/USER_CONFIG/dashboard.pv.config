# Schemas with datatype for initialization
"block_name": "string"
"run_area": "string"
"log_file": "string"
"pv_checks": "array" ; # drc, lvs, antenna, perc...

# Schemas with datatype foreach pv_checks
foreach check $pv_checks {
    "check,violation": "int"
}